# internal

This directory contains internal packages that are private to this project and cannot be imported by external projects.

Planned packages:
- `version/` - Version detection and comparison
- `deploy/` - Deployment logic for observability components
- `storage/` - Storage provider implementations
- `olm/` - Operator Lifecycle Manager utilities
- Additional internal packages as needed
